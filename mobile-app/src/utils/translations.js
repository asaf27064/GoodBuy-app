import eng from '../strings/locales/eng.json';
import heb from '../strings/locales/heb.json';

const LANGUAGES = {
    eng,
    heb,
  };
  
  let currentLanguage = 'heb';

// Unfinished.

/*export const getLanguage = () => currentLanguage;

export const getT = (key) => {

  };*/